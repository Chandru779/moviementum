# Movie data provider: cinemeta (default, free, no key required)
REACT_APP_MOVIE_PROVIDER=cinemeta

# Optional TMDB key — improves cast photos via TMDB credits (falls back to Wikipedia)
# REACT_APP_TMDB_API_KEY=your_tmdb_api_key_here
